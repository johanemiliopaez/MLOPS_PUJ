services:
  api:
    build:
      context: ..
      dockerfile: Docker/Dockerfile
    container_name: data-p2-api
    ports:
      - "8888:80"
    volumes:
      - ../data:/app/data
    restart: unless-stopped
